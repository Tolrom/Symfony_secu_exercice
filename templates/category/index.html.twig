{% extends 'base.html.twig' %}

{% block title %}Ajouter une catégorie{% endblock %}

{% block body %}
    {{ form_start(formulaire) }}
    {{ form_end(formulaire) }}
    <p>{{ message }}</p>
{% endblock %}
